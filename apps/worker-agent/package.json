{
  "name": "worker-agent",
  "version": "0.1.0",
  "description": "Cloudflare Agents SDK supervisor for Raycast orchestrator",
  "type": "module",
  "main": "src/agent.ts",
  "dependencies": {
    "agents": "workspace:*",
    "@cloudflare/workers-types": "^4.20240919.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "type-check": "tsc --noEmit"
  }
}
